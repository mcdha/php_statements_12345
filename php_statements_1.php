<!DOCTYPE html>

    <?php
    $word1 = 'Class';
    $word2 = 'Kodego';
    $word3 = 'Top';

    if (strlen($word1) > 5) {
        echo "$word1 is more than 5 characters.";
    } else {
        echo "$word1 is not more than 5 characters.";
    }

    echo "<br>";

    if (strlen($word2) > 5) {
        echo "$word2 is more than 5 characters.";
    } else {
        echo "$word2 is not more than 5 characters.";
    }

    echo "<br>";

    if (strlen($word3) > 5) {
        echo "$word3 is more than 5 characters.";
    } else {
        echo "$word3 is not more than 5 characters.";
    }
    ?>

